# ── Core ──────────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── YouTube 자막 취득 (이중 안전장치) ─────────────────────────────────────────
yt-dlp>=2024.5.1
youtube-transcript-api>=0.6.2

# ── HTTP (Gemini REST 호출, 표준 라이브러리 requests 사용) ────────────────────
requests>=2.31.0
# ※ Anthropic SDK 불필요 — Gemini API는 무료 REST로 직접 호출
